// Two sums

#include <bits/stdc++.h>
#include <limits>
using namespace std;

vector<int> solve(vector<int> a, int k) {
    for(int i=0;i<a.size();i++) {
	
    }
    return [];
}


int main() {
    int n, k, tmp;
    vector<int> a;
    cin>>n;
    while(n--) {
	cin>>tmp;
	a.push_back(tmp);
    }

    return 0;
}
